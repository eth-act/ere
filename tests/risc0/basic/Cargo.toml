[package]
name = "ere-test-risc0-guest"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { git = "https://github.com/risc0/risc0.git", tag = "v3.0.0", default-features = false, features = ["std", "unstable"] }
test-utils = { path = "../../../crates/test-utils" }
